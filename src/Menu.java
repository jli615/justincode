public class Menu {
}


//have a dict that connects to input, to a thread of every challenge, threads would be created outside the menu, where we could refer to them, menu = input the value, search through the dict of inputs of threads, run that thread for the start
//take input, dict, etc.

